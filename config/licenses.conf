<licenses>
  <license id="{{cfg.license_id}}" sig="{{cfg.licence_sig}}">
    <requestor>{{cfg.license_requestor}}</requestor>
    <graphite>true</graphite>
    <expiry>{{cfg.expiry}}</expiry>
    <company>{{cfg.company}}</company>
    <created_by_user>{{cfg.created_by_user}}</created_by_user>
    <max_streams>{{cfg.max_streams}}</max_streams>
    <lua_extension>{{cfg.lua_extension}}</lua_extension>
  </license>
</licenses>
